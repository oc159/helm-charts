apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-web-app-deployment.fullname" . }}
  labels:
    {{- include "go-web-app-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-web-app-deployment.selectorLabels" . | nindent 4 }}
